---
title: 'Close Accounting Period'
description: 'Close an accounting period to prevent new transactions'
api: 'POST /service/general-ledger/accounting-periods/{period_id}/close'
---

## Request

Close an accounting period. By default, the period cannot be closed if there are unposted entries. Use `force=true` to override this check.

### Headers

<ParamField header="Authorization" type="string" required>
  JWT authentication token. Format: `jwt YOUR_TOKEN`
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`
</ParamField>

### Path Parameters

<ParamField path="period_id" type="string" required>
  The unique identifier of the accounting period to close
</ParamField>

### Body Parameters

<ParamField body="close_type" type="string" default="all">
  Which sub-ledgers to close. Options:
  - `all` - Close all sub-ledgers
  - `ap` - Close Accounts Payable only
  - `ar` - Close Accounts Receivable only
  - `payroll` - Close Payroll only
</ParamField>

<ParamField body="force" type="boolean" default="false">
  Set to `true` to close even if there are unposted entries
</ParamField>

<Warning>
  Using `force=true` will close the period even if there are unposted documents. These documents will remain in their current state and cannot be posted until the period is reopened.
</Warning>

<RequestExample>
```bash cURL
curl -X POST https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/close \
  -H "Authorization: jwt YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "close_type": "all",
    "force": false
  }'
```

```javascript JavaScript
const response = await fetch('https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/close', {
  method: 'POST',
  headers: {
    'Authorization': 'jwt YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    close_type: 'all',
    force: false
  })
});

const result = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/close',
    headers={
        'Authorization': 'jwt YOUR_TOKEN',
        'Content-Type': 'application/json'
    },
    json={
        'close_type': 'all',
        'force': False
    }
)

result = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 OK
{
  "period_id": "a3UM4R8nYh33A6hgda",
  "period_name": "January 2024",
  "is_closed": true,
  "closed_at": "2024-02-01T10:00:00Z",
  "closed_by": "user_123",
  "close_type": "all"
}
```

```json 409 Conflict
{
  "error": {
    "code": "UNPOSTED_ENTRIES",
    "message": "Cannot close period with unposted entries",
    "details": {
      "journal_entries": {
        "draft": 3,
        "pending_approval": 2
      },
      "bills": {
        "draft": 1
      }
    }
  }
}
```
</ResponseExample>

## Close Process

Before closing a period, the system checks for unposted documents across all document types:
- Journal Entries
- Bills
- Invoices
- Bill Payments
- Invoice Payments
- Credit Memos

Documents in the following statuses are considered "unposted":
- Draft
- Pending Approval
- On Hold
- Approved (but not yet posted)

Use the [Check Unposted Entries](/api-reference/general-ledger/accounting-periods/unposted-entries) endpoint to review these documents before closing.
