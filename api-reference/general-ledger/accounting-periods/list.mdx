---
title: 'List Accounting Periods'
description: 'List all accounting periods with optional filters'
api: 'GET /service/general-ledger/accounting-periods'
---

## Request

Retrieve a list of accounting periods with optional filtering.

### Headers

<ParamField header="Authorization" type="string" required>
  JWT authentication token. Format: `jwt YOUR_TOKEN`
</ParamField>

### Query Parameters

<ParamField query="fiscal_year_id" type="string">
  Filter by fiscal year (e.g., "FY2024")
</ParamField>

<ParamField query="is_closed" type="boolean">
  Filter by closed status. Set to `false` to get only open periods
</ParamField>

<ParamField query="start_date" type="string">
  Filter periods starting on or after this date (YYYY-MM-DD)
</ParamField>

<ParamField query="end_date" type="string">
  Filter periods ending on or before this date (YYYY-MM-DD)
</ParamField>

<RequestExample>
```bash cURL
curl -X GET "https://kube.acho.io/service/general-ledger/accounting-periods?fiscal_year_id=FY2024&is_closed=false" \
  -H "Authorization: jwt YOUR_TOKEN"
```

```javascript JavaScript
const params = new URLSearchParams({
  fiscal_year_id: 'FY2024',
  is_closed: 'false'
});

const response = await fetch(`https://kube.acho.io/service/general-ledger/accounting-periods?${params}`, {
  method: 'GET',
  headers: {
    'Authorization': 'jwt YOUR_TOKEN'
  }
});

const periods = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://kube.acho.io/service/general-ledger/accounting-periods',
    headers={
        'Authorization': 'jwt YOUR_TOKEN'
    },
    params={
        'fiscal_year_id': 'FY2024',
        'is_closed': 'false'
    }
)

periods = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 OK
{
  "data": [
    {
      "period_id": "a3UM4R8nYh33A6hgda",
      "period_name": "January 2024",
      "fiscal_year_id": "FY2024",
      "start_date": "2024-01-01",
      "end_date": "2024-01-31",
      "quarter": 1,
      "is_adjustment_period": false,
      "is_closed": false
    },
    {
      "period_id": "a3UM4R8nYh33A6hgdb",
      "period_name": "February 2024",
      "fiscal_year_id": "FY2024",
      "start_date": "2024-02-01",
      "end_date": "2024-02-29",
      "quarter": 1,
      "is_adjustment_period": false,
      "is_closed": false
    }
  ],
  "pagination": {
    "total": 12,
    "limit": 20,
    "offset": 0,
    "has_more": false
  }
}
```
</ResponseExample>
