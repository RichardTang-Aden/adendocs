---
title: 'Reopen Accounting Period'
description: 'Reopen a previously closed accounting period'
api: 'POST /service/general-ledger/accounting-periods/{period_id}/reopen'
---

## Request

Reopen a closed accounting period to allow additional transactions. A reason is required for audit purposes.

### Headers

<ParamField header="Authorization" type="string" required>
  JWT authentication token. Format: `jwt YOUR_TOKEN`
</ParamField>

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`
</ParamField>

### Path Parameters

<ParamField path="period_id" type="string" required>
  The unique identifier of the accounting period to reopen
</ParamField>

### Body Parameters

<ParamField body="reason" type="string" required>
  Reason for reopening the period. Required for audit trail.
</ParamField>

<ParamField body="reopen_type" type="string" default="all">
  Which sub-ledgers to reopen. Options:
  - `all` - Reopen all sub-ledgers
  - `ap` - Reopen Accounts Payable only
  - `ar` - Reopen Accounts Receivable only
  - `payroll` - Reopen Payroll only
</ParamField>

<Note>
  Only soft-closed periods can be reopened. Hard-closed periods cannot be reopened.
</Note>

<RequestExample>
```bash cURL
curl -X POST https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/reopen \
  -H "Authorization: jwt YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "reason": "Need to post adjusting entry for vendor invoice",
    "reopen_type": "all"
  }'
```

```javascript JavaScript
const response = await fetch('https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/reopen', {
  method: 'POST',
  headers: {
    'Authorization': 'jwt YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    reason: 'Need to post adjusting entry for vendor invoice',
    reopen_type: 'all'
  })
});

const result = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://kube.acho.io/service/general-ledger/accounting-periods/a3UM4R8nYh33A6hgda/reopen',
    headers={
        'Authorization': 'jwt YOUR_TOKEN',
        'Content-Type': 'application/json'
    },
    json={
        'reason': 'Need to post adjusting entry for vendor invoice',
        'reopen_type': 'all'
    }
)

result = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 OK
{
  "period_id": "a3UM4R8nYh33A6hgda",
  "period_name": "January 2024",
  "is_closed": false,
  "reopened_at": "2024-02-05T14:00:00Z",
  "reopened_by": "user_123",
  "reason": "Need to post adjusting entry for vendor invoice",
  "reopen_type": "all"
}
```

```json 409 Conflict
{
  "error": {
    "code": "HARD_CLOSED",
    "message": "Cannot reopen a hard-closed period"
  }
}
```

```json 400 Bad Request
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Reason is required for reopening a period"
  }
}
```
</ResponseExample>
